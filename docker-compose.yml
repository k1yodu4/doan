version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: bao_shop_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: bao_shop
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres_root_2026
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: bao_shop_pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@baoshop.com
      PGADMIN_DEFAULT_PASSWORD: admin_2026
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  postgres_data: